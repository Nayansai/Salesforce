@isTest

public class TestVerifyDate {

    @isTest

    static void testMethod1() {

        Date d = VerifyDate.CheckDates(System.today(), System.today() + 1);

        Date d1 = VerifyDate.CheckDates(System.today(), System.today() + 60);

        // Add your assertions or other test logic here

        System.assertEquals(System.today() + 1, d, 'Dates should be equal');

        System.assertEquals(System.today() + 60, d1, 'Dates should be equal');

    }

}